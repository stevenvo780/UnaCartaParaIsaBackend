[users]
# Desactivar página de inicio por defecto
default_theme = dark

[auth.anonymous]
# Permitir acceso anónimo para ver dashboards
enabled = true
org_name = Main Org.
org_role = Viewer
hide_version = true

[dashboards]
# Configurar el home dashboard por defecto
default_home_dashboard_path = /var/lib/grafana/dashboards/0-home-overview.json

[security]
# Admin user/password
admin_user = admin
admin_password = admin

[server]
# Configuración del servidor
root_url = %(protocol)s://%(domain)s:%(http_port)s/

[feature_toggles]
# Deshabilitar la página de bienvenida/onboarding
enable = 

/**
 * Enumeration of notification types used in the UI.
 */
export enum NotificationType {
  INFO = "info",
  WARNING = "warning",
  ERROR = "error",
  SUCCESS = "success",
}

/**
 * Type for notification type values.
 */
export type NotificationTypeValue = NotificationType;
